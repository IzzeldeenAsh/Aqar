const i18nConfig = {
    locales: ['en', 'ar'], // Include 'en' here
    defaultLocale: 'en', // This should match one of the locales in the array
    prefixDefault: true
}

module.exports = i18nConfig;
